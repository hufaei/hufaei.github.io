{
  "name": "hufaei-github-pages",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/prune-private-pages.mjs",
    "preview": "astro preview",
    "check": "astro check && tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@astrojs/check": "latest",
    "astro": "latest",
    "typescript": "latest"
  },
  "devDependencies": {
    "@types/node": "^25.7.0",
    "vitest": "latest"
  }
}
